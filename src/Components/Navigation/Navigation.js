import React from 'react';

const Navigation = () => {
	return (
		<nav style={{ display: 'flex', justifyContent: 'flex-end', padding: '10px' }}>
			<p className="white f3 link dim black underline pa3 pointer">Sign in</p>
		</nav>
	);
};

export default Navigation;
